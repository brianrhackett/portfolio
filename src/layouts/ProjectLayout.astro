---
import Layout from './Layout.astro';

interface Props {
	title: string;
	description: string;
}

const {
	title,
	description
} = Astro.props;
---

<Layout
	title={`${title} | Brian Hackett`}
	description={description}
>
	<article>
		<slot />
	</article>
</Layout>